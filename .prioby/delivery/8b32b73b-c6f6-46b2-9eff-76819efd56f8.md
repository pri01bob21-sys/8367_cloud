You are a senior full-stack + DevOps engineer.

You are a senior full-stack + DevOps engineer.

I want to build a complete Network Management System (NMS) with backend, frontend, database, and messaging system.

---

# 🧠 CORE REQUIREMENT

PostgreSQL MUST NOT be installed locally.

It MUST run inside Docker using docker-compose.

All services must communicate via Docker network.

---

# ⚙️ BACKEND (Node.js + Express)

1. Create a Node.js backend using Express.

2. Create POST API:
   Route: /update_switch_data

3. This API is called by network switches (NOT frontend).

4. Sample payload:
   {
   "ip": "192.168.1.10",
   "macId": "AA:BB:CC:DD:EE:FF",
   "hostname": "Switch-1",
   "status": "online",
   "uptime": 123456,
   "ports": 24
   }

5. Store data in PostgreSQL (running inside Docker).

6. Table: switches
   Columns:

* id (SERIAL PRIMARY KEY)
* ip (UNIQUE, NOT NULL)
* mac_id
* hostname
* status
* uptime
* ports
* last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP

7. If same IP comes:
   → UPDATE instead of INSERT (UPSERT)

8. Create GET API:
   Route: /switches

9. Add:

* MVC structure
* Logging
* Error handling

10. Add health check:
    GET /health

---

# 🗄️ DATABASE (PostgreSQL via Docker ONLY)

* Must run as a Docker container

* Use official postgres image

* No local installation allowed

* Use environment variables:
  POSTGRES_DB
  POSTGRES_USER
  POSTGRES_PASSWORD

* Persist data using Docker volume:
  postgres_data

---

# 📡 NATS (Messaging)

* Use NATS inside Docker

* When /update_switch_data is called:
  → Publish event "switch.updated"

* Include full payload in event

---

# 🎨 FRONTEND (React)

Create an NMS Dashboard UI:

Features:

* Table listing all switches

* Columns:

  * IP
  * MAC ID
  * Hostname
  * Status (green/red indicator)
  * Uptime
  * Ports
  * Last Updated

* Auto refresh every 5 seconds

* Show:
  Online → Green dot
  Offline → Red dot

* Use:

  * React (functional components)
  * Axios
  * useEffect

* Add:

  * Loading state
  * Clean CSS (no UI libraries required)

---

# 🐳 DOCKER SETUP (VERY IMPORTANT)

Use docker-compose.yml

## Services:

1. backend

   * Build from ./backend
   * Port: 5000
   * Depends on postgres & nats

2. frontend

   * Build from ./frontend
   * Port: 3000

3. postgres

   * Image: postgres:latest
   * Container name: nms_postgres
   * Use volume: postgres_data
   * Expose port: 5432

4. pgweb

   * Image: sosedoff/pgweb
   * Port: 8081
   * Connect to postgres container

5. nats

   * Image: nats
   * Port: 4222

---

# 🌐 NETWORK

* Create custom network:
  nms_network

* All containers must use this network

---

# 💾 VOLUMES

* postgres_data (for DB persistence)

---

# 🔐 ENV VARIABLES (Backend)

Use:

* DB_HOST=postgres   (IMPORTANT: service name, not localhost)
* DB_PORT=5432
* DB_USER
* DB_PASSWORD
* DB_NAME
* NATS_URL=nats://nats:4222

---

# 📁 PROJECT STRUCTURE

nms-project/
backend/
frontend/
docker-compose.yml

---

# 🎯 OUTPUT REQUIRED

Generate:

1. Complete backend code
2. Complete frontend code
3. PostgreSQL table creation logic
4. NATS integration
5. docker-compose.yml
6. Dockerfile (backend & frontend)
7. Step-by-step run instructions

---

# ⚠️ IMPORTANT RULES

* PostgreSQL MUST run only inside Docker
* Backend must connect using service name "postgres"
* No localhost usage for DB connection
* Use async/await clean code
* Production-like structure

---

Generate complete working project.
Do not skip anything.

— Prioby: delivery metadata for agents / humans.
